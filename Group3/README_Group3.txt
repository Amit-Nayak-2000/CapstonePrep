Instructions to run our program.
Navigate to the MATLAB folder (double click the folder "MATLAB", in Matlab).
Open GUI.m using MATLAB and then click run. 
This will open the GUI in a new window.
Set the patient parameters and click BUILD.
You should see a message when the code has finished running.
Click rebuild in SOLIDWORKS to update the CAD. 
Click Exit Designer to close the GUI.
